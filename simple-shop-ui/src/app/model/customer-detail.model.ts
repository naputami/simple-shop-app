export interface CustomerDetail {
    id: string;
    name: string;
    imgUrl: string;
    custCode: string;
    lastOrderDate: string;
    active: boolean;
    address: string;
    phoneNumber: string;
}